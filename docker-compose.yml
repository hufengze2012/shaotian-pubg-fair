services:
  pubg-web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.json:/app/config.json
      - ./pubg_cli_cache.json:/app/pubg_cli_cache.json
    restart: unless-stopped
